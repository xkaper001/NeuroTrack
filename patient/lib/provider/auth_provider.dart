import 'package:flutter/material.dart';

class AuthProvider extends ChangeNotifier {
  // The provider will act like a combination of service and controller.
  // It will make use of the repository to perform the actual authentication and then notify the listeners of the state of the authentication process.
  // AuthProvider({ required this.authRepository });
}
